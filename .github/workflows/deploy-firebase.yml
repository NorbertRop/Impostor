name: Deploy to Firebase

on:
  push:
    branches:
      - main
    paths:
      - "frontend/**"
      - "functions/**"
      - "firebase.json"
      - "firestore.rules"
      - "firestore.indexes.json"
      - ".github/workflows/deploy-firebase.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: Firebase

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: 🔨 Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: 🔐 Create .env file with Firebase config
        working-directory: frontend
        run: |
          echo "VITE_FIREBASE_API_KEY=${{ secrets.VITE_FIREBASE_API_KEY }}" >> .env
          echo "VITE_FIREBASE_AUTH_DOMAIN=${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}" >> .env
          echo "VITE_FIREBASE_PROJECT_ID=${{ secrets.VITE_FIREBASE_PROJECT_ID }}" >> .env
          echo "VITE_FIREBASE_STORAGE_BUCKET=${{ secrets.VITE_FIREBASE_STORAGE_BUCKET }}" >> .env
          echo "VITE_FIREBASE_MESSAGING_SENDER_ID=${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID }}" >> .env
          echo "VITE_FIREBASE_APP_ID=${{ secrets.VITE_FIREBASE_APP_ID }}" >> .env
          echo "VITE_API_URL=${{ secrets.VITE_API_URL }}" >> .env

      - name: 🏗️ Build frontend
        working-directory: frontend
        run: npm run build

      - name: 🔧 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 🐍 Setup Python Functions environment
        working-directory: functions
        run: |
          python3.12 -m venv venv
          source venv/bin/activate
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📝 Create Firebase service account file
        run: |
          echo '${{ secrets.FIREBASE_SERVICE_ACCOUNT_IMPOSTOR_6320A }}' > $HOME/firebase-key.json
          echo "GOOGLE_APPLICATION_CREDENTIALS=$HOME/firebase-key.json" >> $GITHUB_ENV

      - name: 🔥 Install Firebase CLI
        run: npm install -g firebase-tools

      - name: 🚀 Deploy to Firebase
        run: |
          # Deploy hosting first (always works)
          firebase deploy --only hosting,firestore:rules,firestore:indexes --project impostor-6320a --non-interactive

          # Try to deploy functions (will fail if permissions not granted yet)
          # Remove the '|| true' once permissions are fixed
          firebase deploy --only functions --project impostor-6320a --non-interactive || echo "⚠️ Functions deployment skipped - permissions needed (see .github/FIX_PERMISSIONS.md)"

      - name: 🧹 Cleanup
        if: always()
        run: rm -f $HOME/firebase-key.json
